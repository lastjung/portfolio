# 📋 포트폴리오 코드 구조 최적화 및 분리 계획서

## 1. 개요 (Overview)

- **목표**: `index.html`의 단일 파일 구조를 모듈화하여 가독성, 유지보수성, 성능을 극대화함.
- **핵심 전략**:
  1.  **CSS 분리**: 인라인 스타일을 전역 디자인 시스템 파일로 통합.
  2.  **JS 모듈화**: 스크립트 로직을 기능별(애니메이션, 데이터, 엔진)로 분리.
  3.  **컴포넌트화**: 각 섹션(Lab, Lab2 등)을 독립된 파일로 분리 (향후 Vite 등 빌드 도구 사용 시).

## 2. 제안하는 폴더 구조 (Proposed Structure)

```text
portfolio/
├── index.html              # 각 섹션을 담는 메인 레이아웃 (Shell)
├── docs/
│   └── code-splitting-plan.md  # 본 계획서 문서
├── css/
│   ├── main.css            # 기본 레이아웃 및 Tailwind 커스텀 유틸리티
│   └── components.css       # 카드 스타일, 글래스모피즘 등 컴포넌트 전용 스타일
├── js/
│   ├── main.js             # 공통 인터랙션 및 Scroll Observer
│   ├── fourier-bg.js        # 3D 배경 시뮬레이션 엔진
│   └── data-inventory.js    # 프로젝트 리스트 관리 (JSON 형태)
└── assets/                 # 최적화된 이미지 및 에셋 관리
```

## 3. 세부 실행 단계 (Action Plan)

### Step 1: 스타일 시스템 외부화 (CSS Extraction)

- `index.html` 상단의 방대한 `<style>` 블록을 `css/main.css`로 이동.
- Tailwind 커스텀 설정(`tailwind.config.js`)을 도입하여 하드코딩된 컬러 토큰 정리.

### Step 2: 스크립트 모듈화 (JS Modularization)

- 바닥글에 집중된 모든 JavaScript 로직을 `js/main.js`로 분리.
- 특히 `Reveal on Scroll` 로직과 `Mobile Navigation` 인터랙션을 별도 모듈로 관리.

### Step 3: 데이터 기반 렌더링 (Data-Driven UI)

- 각 카드의 HTML 코드가 반복적이므로, 프로젝트 정보를 `data-inventory.js`에 객체 배열로 저장.
- JS 루프를 통해 카드를 자동 생성하여 `index.html`의 코드량을 60% 이상 절감.

---

## 4. 기대 효과 (Expected Outcomes)

- **파일 크기 감소**: `index.html`을 1,100라인 -> 300라인 이하로 축소.
- **관리 효율성**: 특정 섹션의 디자인이나 프로젝트 내용을 수정할 때 해당 파일/데이터만 수정 가능.
- **로딩 성능**: 필요한 정적 파일만 병렬로 로드하여 초기 렌더링 최적화.

---

© 2026 ENGINEERING.PORTFOLIO | Architecture Documentation.
